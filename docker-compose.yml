version: '3.8'

services:
  tabby:
    image: tabbyml/tabby:nightly
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - TABBY_WEBSERVER_ADDRESS=0.0.0.0:8080
    restart: unless-stopped
